import React from 'react';
import { Title } from '../../components/base/Title';

export const Menu = () => {
    return (
        <div style={{ textAlign: 'center' }}>
            <Title label='メニュー' />
        </div>
    );
};