import React from 'react';
import { Title } from '../../components/base/Title';

export const New = () => {
    return (
        <div style={{ textAlign: 'center' }}>
            <Title label='新規登録' />
        </div>
    );
};